import { ScrollView, Text, View } from "react-native";
import { Products } from "../components";

export const Home = () => {
  return (
    <View>
      <Text>Home</Text>
      <Products />
    </View>
  );
};
