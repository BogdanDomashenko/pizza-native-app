import { Button } from "react-native";
import styled from "styled-components/native";

export const ButtondStyled = styled.Button`
  color: aqua;
  background-color: #00aeef;
  border: 1px solid #fff;
`;
/* 
const ButtonUi = (variant, ...rest) => {
  return (
    <Button {...rest} />
  )
} */
